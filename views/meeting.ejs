<!DOCTYPE html>
<html>
    <!-- script we made -->
    <script type="text/javascript" src="javascript/meeting_event.js"></script>
    <script type="text/javascript" src="javascript/meeting_user_create.js"></script>
    <!-- socket io -->
    <script src="/socket.io/socket.io.js"></script>
    <script src="http://code.jquery.com/jquery-1.11.1.js"></script>

    <head>
    <title>会議中</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='stylesheets/meeting.css' />   
  </head>
  
    <header class="site-header">
      <p class="site-logo">会議促進システム</p>
    </header>
    <body>
      
    <!-- <text >会議名<%= title %>が進行中</text>
    <p>現在 <%= title %>名が参加中</text> -->
    <div id = top></div>
    <div id = messages></div>

    <script>
        var message='test';
        var amount='発話量';
        var confference ='現在会議中です';
        var participant =0;
        var user_id=0;

        createtitle();

        create_user();
        create_user();

        function createtitle(){
          var top_div = document.getElementById("top");
          text=''
          text+='<div class="now" id = "text">'+confference+'</div>';
          text+='<div class="member" id = "participant">現在'+participant+'名が参加しています</div>';
          top_div.innerHTML += text;
        }
  
        function create_user(){
          var message_div = document.getElementById("messages");
          text=''
          text+='<div class="chat-box">';
          text+='<div class="chat-face"><img src="/images/chat/my.png" alt="" /></div>';
          text+='<div class="chat-area">';
          text+='<div class="chat-hukidashi" id = "text">'+message+'</div>';
          text+='<div class="speak-amount" id = "speak-amount">'+amount;
          text+='<div class="meter"><meter max="100" low="20" high="40" value="100">';
          text+='</div>';
          text+='</div>';
          text+='</div>';
          message_div.innerHTML += text;
          num+=1;
      }

      function update_user(){
          message_div = document.getElementById("messages");
          message='clicked';
          text=''
          text+='<div class="chat-box">';
          text+='<div class="chat-face"><img src="/images/chat/my.png" alt="" /></div>';
          text+='<div class="chat-area">';
          text+='<div class="chat-hukidashi" id = "text">'+message+'</div>';
          text+='<div class="speak-amount" id = "speak-amount">'+amount;
          text+='<div class="meter"><meter max="100" low="20" high="40" value="100">';
          text+='</div>';
          text+='</div>';
          text+='</div>';
          message_div.innerHTML = text;
      }
  
      function yesno(){
        update_user();
      }
  
  
      </script>
   
    <INPUT type="button" value="No" style="background:#FFFFFF" onClick="yesno()">
    
    <footer>
      <address>TeamA Bright Light</address>
      <address>KONICA MINOLTA Hackathon.</address>
    </footer>
  </body>
</html>
